(() => { document.querySelectorAll('.history-chart').forEach(canvas => { const history=JSON.parse(canvas.dataset.history||'[]'); const draw=()=>{const r=canvas.getBoundingClientRect(),d=devicePixelRatio||1,w=r.width,h=240,p={l:20,r:16,t:25,b:30},c=canvas.getContext('2d');canvas.width=w*d;canvas.height=h*d;c.scale(d,d);c.clearRect(0,0,w,h);const max=Math.max(1,...history.flatMap(x=>[x.income,x.expense]));c.strokeStyle='#e8ebe5';for(let i=0;i<4;i++){let y=p.t+i*(h-p.t-p.b)/3;c.beginPath();c.moveTo(p.l,y);c.lineTo(w-p.r,y);c.stroke()}const line=(key,color)=>{c.strokeStyle=color;c.lineWidth=3;c.beginPath();history.forEach((x,i)=>{let px=p.l+i*(w-p.l-p.r)/Math.max(1,history.length-1),py=p.t+(h-p.t-p.b)*(1-x[key]/max);i?c.lineTo(px,py):c.moveTo(px,py)});c.stroke()};line('income','#5e9d72');line('expense','#ef8354');c.fillStyle='#8a958d';c.font='11px sans-serif';history.forEach((x,i)=>c.fillText(x.label,p.l+i*(w-p.l-p.r)/Math.max(1,history.length-1)-10,h-8));};draw();addEventListener('resize',draw);});})();
